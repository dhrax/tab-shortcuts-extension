{
  "name": "tab-shortcuts-extension",
  "version": "0.1.0",
  "description": "Extension to manage tabs with keyboard shortcuts in Brave.",
  "type": "module",
  "scripts": {
    "build": "tsc && copyfiles -u 1 \"public/**/*\" dist",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.283",
    "copyfiles": "^2.4.1",
    "typescript": "^5.7.2"
  }
}